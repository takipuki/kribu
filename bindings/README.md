# Python Bindings

This directory contains the code that connects the `engine` C++ library to Python using `nanobind`.

## Structure

- `src/kribu_ext.cpp`: The nanobind module definitions that expose C++ functions and classes to Python.
